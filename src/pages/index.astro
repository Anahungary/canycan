---
// src/pages/index.astro - VERSIÓN QUE CARGA ARTÍCULOS REALES AUTOMÁTICAMENTE
import BaseLayout from '../layouts/BaseLayout.astro';
import Container from '../components/ui/Container.astro';
import HeroBanner from '../components/home/HeroBanner.astro';
import Button from '../components/ui/Button.astro';
import Card from '../components/ui/Card.astro';
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';

// 🔍 CARGAR TODOS LOS ARTÍCULOS REALES QUE EXISTEN
let allArticles: any[] = [];
try {
  allArticles = await getCollection('articulos');
  // Filtrar solo artículos publicados y ordenar por fecha
  allArticles = allArticles
    .filter((article: any) => article.data.status === 'published')
    .sort((a: any, b: any) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime());
} catch (error) {
  console.log('No se pudieron cargar los artículos:', error);
  allArticles = [];
}

// Datos del hero principal
const heroData = {
  title: 'La guía definitiva para el bienestar de tu perro',
  subtitle: 'Descubre todo lo que necesitas para mantener a tu mejor amigo feliz, sano y en perfecta forma',
  image: '/images/hero/perro-hero2.jpg',
  category: 'Guía Completa',
  badges: ['Salud', 'Nutrición', 'Ejercicio'],
  primaryCTA: { text: 'Leer artículo', url: '/articulos/guia-bienestar-perros' },
  secondaryCTA: { text: 'Explorar razas', url: 'https://cani2.netlify.app/razas/' }
};

// 🐕 RAZAS ALEATORIAS (simulando las de cani2.netlify.app)
const featuredBreeds = [
  {
    id: 'golden-retriever',
    name: 'Golden Retriever',
    image: '/images/breeds/golden_retriever.jpg',
    type: 'perro',
    description: 'Leal, inteligente y perfecto para familias activas',
    traits: { energy: 4, friendliness: 5, training: 5 }
  },
  {
    id: 'border-collie',
    name: 'Border Collie',
    image: '/images/breeds/border_collie.jpg',
    type: 'perro',
    description: 'El más inteligente, ideal para dueños experimentados',
    traits: { energy: 5, friendliness: 4, training: 5 }
  },
  {
    id: 'beagle',
    name: 'Beagle',
    image: '/images/breeds/beagle.jpg',
    type: 'perro',
    description: 'Amigable, compacto y excelente con niños',
    traits: { energy: 4, friendliness: 5, training: 3 }
  },
  {
    id: 'boston-terrier',
    name: 'Boston Terrier',
    image: '/images/breeds/boston_terrier.jpg',
    type: 'perro',
    description: 'Compañero urbano inteligente y cariñoso',
    traits: { energy: 3, friendliness: 4, training: 4 }
  }
];

// 📖 ARTÍCULOS RECIENTES POR CATEGORÍAS (de los artículos reales)
const getArticlesByCategory = (category: string, limit = 2) => {
  return allArticles
    .filter((article: any) => 
      article.data.category?.toLowerCase().includes(category.toLowerCase()) ||
      article.data.tags?.some((tag: string) => tag.toLowerCase().includes(category.toLowerCase()))
    )
    .slice(0, limit);
};

const recentArticlesByCategory = {
  comportamiento: getArticlesByCategory('comportamiento'),
  entrenamiento: getArticlesByCategory('entrenamiento'),
  cuidado: getArticlesByCategory('cuidado'),
  nuevos_duenos: getArticlesByCategory('nuevos')
};

// Función para formatear fecha
const formatDate = (date: string | Date) => {
  return new Date(date).toLocaleDateString('es-ES', {day: 'numeric', month: 'short'});
};

// Función para obtener color de categoría
const getCategoryColor = (category: string | undefined) => {
  const cat = category?.toLowerCase() || '';
  if (cat.includes('entrenamiento')) return 'bg-[#AFC2D5]/20 text-[#436280]';
  if (cat.includes('comportamiento')) return 'bg-[#F6B89E]/20 text-[#B25E42]';
  if (cat.includes('cuidado')) return 'bg-[#C8D6B9]/20 text-[#5A7251]';
  if (cat.includes('nuevos')) return 'bg-purple-100 text-purple-800';
  return 'bg-gray-100 text-gray-700';
};
---

<BaseLayout
  title="PataDigital - La revista especializada en perros y gatos"
  description="Descubre consejos, guías, comparativas de razas y todo lo que necesitas para el bienestar de tus mascotas."
  bodyClass="homepage"
>
  {/* Hero + Artículos destacados REALES */}
  <section class="bg-gradient-to-br from-[#F4F4F4] to-[#E6ECF2] py-12">
    <Container fluid={true} size="xl">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-stretch">
        {/* Hero Banner */}
        <div class="relative overflow-hidden rounded-2xl shadow-2xl h-full">
          <div class="relative h-full min-h-[680px] overflow-hidden">
            <Image 
              src={heroData.image} 
              alt={heroData.title}
              width={1200}
              height={800}
              class="w-full h-full object-cover"
            />
            <div class="absolute inset-0 bg-gradient-to-tr from-black via-black/40 to-transparent"></div>
            
            <div class="absolute inset-0 flex flex-col justify-end p-8 z-10">
              <div class="flex flex-wrap gap-2 mb-4">
                <span class="bg-[#AFC2D5] text-white px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wide">
                  {heroData.category}
                </span>
                {heroData.badges.map((badge) => (
                  <span class="bg-white/80 text-[#2E2E2E] px-3 py-1 rounded-full text-xs font-medium">
                    {badge}
                  </span>
                ))}
              </div>
              
              <h1 class="text-3xl md:text-4xl font-bold text-white mb-3 drop-shadow-md">
                {heroData.title}
              </h1>
              
              <p class="text-lg text-white/90 mb-6 max-w-xl drop-shadow-sm line-clamp-3">
                {heroData.subtitle}
              </p>
              
              <div class="flex flex-wrap gap-3">
                <Button 
                  href={heroData.primaryCTA.url}
                  variant="primary" 
                  size="lg"
                  class="bg-white text-[#2E2E2E] hover:bg-gray-100 border border-white shadow-md"
                >
                  {heroData.primaryCTA.text}
                </Button>
                
                <Button 
                  href={heroData.secondaryCTA.url}
                  variant="outline" 
                  size="lg"
                  class="border-white text-white hover:bg-white/20 shadow-md"
                >
                  {heroData.secondaryCTA.text}
                </Button>
              </div>
            </div>
          </div>
        </div>

        {/* TODOS LOS ARTÍCULOS REALES QUE EXISTEN */}
        <div class="bg-white rounded-2xl shadow-2xl p-6 flex flex-col h-full">
          <div class="flex items-center justify-between mb-6 pb-2 border-b border-gray-100">
            <h2 class="text-xl font-bold text-[#2E2E2E]">
              {allArticles.length > 0 ? `Todos los artículos (${allArticles.length})` : 'Artículos destacados'}
            </h2>
            <Button href="/blog" variant="text" size="sm">Ver blog</Button>
          </div>
          
          {allArticles.length > 0 ? (
            <div class="flex-grow overflow-y-auto max-h-[600px] pr-2">
              <div class="space-y-3">
                {allArticles.map((article: any) => (
                  <a href={`/articulos/${article.slug}`} class="group">
                    <Card variant="default" padding="none" class="hover:shadow-md transition-shadow">
                      <div class="flex items-center p-3">
                        <div class="w-16 h-16 flex-shrink-0 rounded-lg overflow-hidden bg-gray-200">
                          {article.data.image ? (
                            <Image 
                              src={article.data.image} 
                              alt={article.data.title}
                              width={64}
                              height={64}
                              class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
                            />
                          ) : (
                            <div class="w-full h-full flex items-center justify-center text-gray-400">
                              📄
                            </div>
                          )}
                        </div>
                        
                        <div class="ml-3 flex-1 min-w-0">
                          <div class="flex items-center gap-2 mb-1">
                            <span class={`text-xs font-medium px-2 py-0.5 rounded-full ${getCategoryColor(article.data.category)}`}>
                              {article.data.category || 'General'}
                            </span>
                            <span class="text-xs text-gray-400">•</span>
                            <span class="text-xs text-gray-500">{article.data.readingTime || 5} min</span>
                          </div>
                          
                          <h3 class="text-sm font-bold text-[#2E2E2E] group-hover:text-[#AFC2D5] transition-colors line-clamp-2 mb-1">
                            {article.data.title}
                          </h3>
                          
                          {article.data.description && (
                            <p class="text-xs text-gray-600 line-clamp-2 mb-1">
                              {article.data.description}
                            </p>
                          )}
                          
                          <div class="flex items-center text-xs text-gray-500">
                            <span>{article.data.author || 'Editor'}</span>
                            <span class="mx-1">•</span>
                            <span>{formatDate(article.data.date)}</span>
                          </div>
                        </div>
                      </div>
                    </Card>
                  </a>
                ))}
              </div>
            </div>
          ) : (
            <div class="flex-grow flex items-center justify-center text-center py-12">
              <div>
                <p class="text-gray-500 mb-4">No hay artículos disponibles aún</p>
                <Button href="/admin/" variant="primary" size="sm">
                  Crear artículo
                </Button>
              </div>
            </div>
          )}
          
          <div class="text-center pt-4 mt-4 border-t border-gray-100">
            <Button href="/blog" variant="primary" size="sm">
              Ver todos en el blog
            </Button>
          </div>
        </div>
      </div>
    </Container>
  </section>
  
  {/* Razas destacadas - ENLACE A CANI2.NETLIFY.APP */}
  <section class="py-12 bg-white">
    <Container>
      <div class="text-center mb-10">
        <h2 class="text-2xl font-bold mb-3">Descubre la raza perfecta para ti</h2>
        <p class="text-gray-600 max-w-2xl mx-auto">
          Compara características, temperamento y necesidades de las diferentes razas para encontrar tu compañero ideal
        </p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        {featuredBreeds.map(breed => (
          <a href="/razas/" class="group" target="_blank" rel="noopener">
            <Card variant="default" padding="none">
              <div class="relative h-48">
                <Image 
                  src={breed.image} 
                  alt={breed.name}
                  width={400}
                  height={300}
                  class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
                />
                <div class="absolute top-3 left-3 bg-white/80 text-[#2E2E2E] text-xs uppercase tracking-wider px-2 py-1 rounded-full">
                  {breed.type}
                </div>
              </div>
              <div class="p-4">
                <h3 class="text-lg font-bold group-hover:text-[#AFC2D5] transition-colors">{breed.name}</h3>
                <p class="text-sm text-gray-600 mt-1 mb-3">{breed.description}</p>
                
                <div class="space-y-1 text-xs">
                  <div class="flex justify-between">
                    <span>Energía:</span>
                    <span>{'★'.repeat(breed.traits.energy)}{'☆'.repeat(5-breed.traits.energy)}</span>
                  </div>
                  <div class="flex justify-between">
                    <span>Amistad:</span>
                    <span>{'★'.repeat(breed.traits.friendliness)}{'☆'.repeat(5-breed.traits.friendliness)}</span>
                  </div>
                </div>
              </div>
            </Card>
          </a>
        ))}
      </div>
      
      <div class="text-center mt-8">
        <a 
          href="/razas/" 
          target="_blank" 
          rel="noopener"
          class="inline-flex items-center px-6 py-3 bg-[#AFC2D5] hover:bg-[#9DB3C6] text-white rounded-lg text-lg font-medium transition-colors duration-200"
        >
          Ir al comparador de razas
        </a>
      </div>
        </Button>
      </div>
    </Container>
  </section>
    
  {/* Artículos recientes POR SECCIONES - DINÁMICO */}
  <section class="py-12 bg-[#FAFAFA]">
    <Container>
      <div class="text-center mb-12">
        <h2 class="text-2xl font-bold mb-3">Artículos recientes por tema</h2>
        <p class="text-gray-600">Mantente al día con el contenido más actualizado en cada área</p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {/* Comportamiento */}
        <div class="bg-white rounded-xl p-6 shadow-sm">
          <div class="flex items-center justify-between mb-4">
            <h3 class="text-xl font-bold text-[#2E2E2E] flex items-center">
              🧠 <span class="ml-2">Comportamiento</span>
            </h3>
            <Button href="/comportamiento" variant="text" size="sm">Ver más</Button>
          </div>
          
          <div class="space-y-4">
            {recentArticlesByCategory.comportamiento.length > 0 ? (
              recentArticlesByCategory.comportamiento.map((article: any) => (
                <a href={`/articulos/${article.slug}`} class="flex group">
                  <div class="w-16 h-16 flex-shrink-0 rounded-lg overflow-hidden bg-gray-200">
                    {article.data.image ? (
                      <Image 
                        src={article.data.image} 
                        alt={article.data.title}
                        width={64}
                        height={64}
                        class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
                      />
                    ) : (
                      <div class="w-full h-full flex items-center justify-center text-gray-400">🧠</div>
                    )}
                  </div>
                  <div class="ml-3 flex-1">
                    <h4 class="text-sm font-bold text-[#2E2E2E] group-hover:text-[#AFC2D5] transition-colors line-clamp-2">
                      {article.data.title}
                    </h4>
                    <p class="text-xs text-gray-600 mt-1 line-clamp-2">{article.data.description || article.data.excerpt}</p>
                    <div class="text-xs text-gray-500 mt-1">{article.data.readingTime || 5} min lectura</div>
                  </div>
                </a>
              ))
            ) : (
              <p class="text-gray-500 text-sm text-center py-4">No hay artículos de comportamiento disponibles</p>
            )}
          </div>
        </div>

        {/* Entrenamiento */}
        <div class="bg-white rounded-xl p-6 shadow-sm">
          <div class="flex items-center justify-between mb-4">
            <h3 class="text-xl font-bold text-[#2E2E2E] flex items-center">
              🎓 <span class="ml-2">Entrenamiento</span>
            </h3>
            <Button href="/entrenamiento" variant="text" size="sm">Ver más</Button>
          </div>
          
          <div class="space-y-4">
            {recentArticlesByCategory.entrenamiento.length > 0 ? (
              recentArticlesByCategory.entrenamiento.map((article: any) => (
                <a href={`/articulos/${article.slug}`} class="flex group">
                  <div class="w-16 h-16 flex-shrink-0 rounded-lg overflow-hidden bg-gray-200">
                    {article.data.image ? (
                      <Image 
                        src={article.data.image} 
                        alt={article.data.title}
                        width={64}
                        height={64}
                        class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
                      />
                    ) : (
                      <div class="w-full h-full flex items-center justify-center text-gray-400">🎓</div>
                    )}
                  </div>
                  <div class="ml-3 flex-1">
                    <h4 class="text-sm font-bold text-[#2E2E2E] group-hover:text-[#F6B89E] transition-colors line-clamp-2">
                      {article.data.title}
                    </h4>
                    <p class="text-xs text-gray-600 mt-1 line-clamp-2">{article.data.description || article.data.excerpt}</p>
                    <div class="text-xs text-gray-500 mt-1">{article.data.readingTime || 5} min lectura</div>
                  </div>
                </a>
              ))
            ) : (
              <p class="text-gray-500 text-sm text-center py-4">No hay artículos de entrenamiento disponibles</p>
            )}
          </div>
        </div>

        {/* Cuidado */}
        <div class="bg-white rounded-xl p-6 shadow-sm">
          <div class="flex items-center justify-between mb-4">
            <h3 class="text-xl font-bold text-[#2E2E2E] flex items-center">
              💙 <span class="ml-2">Cuidado</span>
            </h3>
            <Button href="/cuidado" variant="text" size="sm">Ver más</Button>
          </div>
          
          <div class="space-y-4">
            {recentArticlesByCategory.cuidado.length > 0 ? (
              recentArticlesByCategory.cuidado.map((article: any) => (
                <a href={`/articulos/${article.slug}`} class="flex group">
                  <div class="w-16 h-16 flex-shrink-0 rounded-lg overflow-hidden bg-gray-200">
                    {article.data.image ? (
                      <Image 
                        src={article.data.image} 
                        alt={article.data.title}
                        width={64}
                        height={64}
                        class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
                      />
                    ) : (
                      <div class="w-full h-full flex items-center justify-center text-gray-400">💙</div>
                    )}
                  </div>
                  <div class="ml-3 flex-1">
                    <h4 class="text-sm font-bold text-[#2E2E2E] group-hover:text-[#C8D6B9] transition-colors line-clamp-2">
                      {article.data.title}
                    </h4>
                    <p class="text-xs text-gray-600 mt-1 line-clamp-2">{article.data.description || article.data.excerpt}</p>
                    <div class="text-xs text-gray-500 mt-1">{article.data.readingTime || 5} min lectura</div>
                  </div>
                </a>
              ))
            ) : (
              <p class="text-gray-500 text-sm text-center py-4">No hay artículos de cuidado disponibles</p>
            )}
          </div>
        </div>

        {/* Nuevos Dueños */}
        <div class="bg-white rounded-xl p-6 shadow-sm">
          <div class="flex items-center justify-between mb-4">
            <h3 class="text-xl font-bold text-[#2E2E2E] flex items-center">
              🆕 <span class="ml-2">Nuevos Dueños</span>
            </h3>
            <Button href="/nuevos-duenos" variant="text" size="sm">Ver más</Button>
          </div>
          
          <div class="space-y-4">
            {recentArticlesByCategory.nuevos_duenos.length > 0 ? (
              recentArticlesByCategory.nuevos_duenos.map((article: any) => (
                <a href={`/articulos/${article.slug}`} class="flex group">
                  <div class="w-16 h-16 flex-shrink-0 rounded-lg overflow-hidden bg-gray-200">
                    {article.data.image ? (
                      <Image 
                        src={article.data.image} 
                        alt={article.data.title}
                        width={64}
                        height={64}
                        class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
                      />
                    ) : (
                      <div class="w-full h-full flex items-center justify-center text-gray-400">🆕</div>
                    )}
                  </div>
                  <div class="ml-3 flex-1">
                    <h4 class="text-sm font-bold text-[#2E2E2E] group-hover:text-[#5A7251] transition-colors line-clamp-2">
                      {article.data.title}
                    </h4>
                    <p class="text-xs text-gray-600 mt-1 line-clamp-2">{article.data.description || article.data.excerpt}</p>
                    <div class="text-xs text-gray-500 mt-1">{article.data.readingTime || 5} min lectura</div>
                  </div>
                </a>
              ))
            ) : (
              <p class="text-gray-500 text-sm text-center py-4">No hay artículos para nuevos dueños disponibles</p>
            )}
          </div>
        </div>
      </div>

      <div class="text-center mt-8">
        <Button href="/blog" variant="primary" size="lg">
          Ver todos los artículos
        </Button>
      </div>
    </Container>
  </section>

  {/* Newsletter y CTA */}
  <section class="py-16 bg-[#C8D6B9] bg-opacity-20">
    <Container>
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-3xl font-bold mb-4">Únete a nuestra comunidad</h2>
        <p class="text-lg text-gray-700 mb-8">
          Suscríbete para recibir consejos, artículos y novedades sobre el cuidado de mascotas directamente en tu correo.
        </p>
        
        <div class="max-w-md mx-auto">
          <form class="flex flex-col md:flex-row gap-3">
            <input
              type="email"
              placeholder="Tu correo electrónico"
              class="flex-grow px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#AFC2D5] focus:border-transparent"
              required
            />
            <Button type="submit" variant="primary" size="lg" class="whitespace-nowrap">
              Suscribirme
            </Button>
          </form>
          <p class="text-xs text-gray-500 mt-3">
            Prometemos no hacer spam. Puedes darte de baja en cualquier momento.
          </p>
        </div>
      </div>
    </Container>
  </section>
</BaseLayout>